spring:
  application:
    name: expensetracker
  datasource:
    url: jdbc:mysql://localhost:3306/expensetracker_db?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080